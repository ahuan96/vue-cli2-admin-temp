/**
 * mutations
 * 修改state值的唯一方式 （不支持异步）
 */
const add = (state)=>{
  return state.number ++
}

/**
 * @description:
 * @param {*} aaa
 * @param {*} ccc
 * @return {*}
 */
function bb(aaa,ccc){

}

export {add}
